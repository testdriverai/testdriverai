version: 6.0.0
steps:
  - prompt: stop dashcam
    commands:
      - command: exec
        lang: pwsh
        code: |
          npm install dashcam@beta -g
          dashcam -t 'Web Test Recording' -p

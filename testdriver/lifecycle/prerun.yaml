version: 6.0.0
steps:
  - prompt: dashcam
    commands:
      - command: exec
        lang: sh
        code: dashcam start
      - command: exec
        lang: sh
        code: dashcam track --name=TestDriver --type=application --pattern="/tmp/testdriver.log"

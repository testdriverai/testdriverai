```yaml testdriver/exec-sh.yaml highlight={6-17}
version: 6.0.0
steps:
  - prompt: fetch user data from API
    commands:
      - command: exec
        output: user
        lang: sh
        code: |
          curl -s https://jsonplaceholder.typicode.com/users | head -1
      - command: hover-text
        text: Username
        description: input field for username
        action: click
      - command: type
        text: ${OUTPUT.user}
  - prompt: assert that the username field shows valid data
    commands:
      - command: focus-application
        name: Google Chrome
      - command: assert
        expect: >-
          the username field contains data from the API response
```

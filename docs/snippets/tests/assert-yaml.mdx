```yaml testdriver/assert.yaml highlight={6,7}
version: 6.0.0
session: 682f5d92811bd5a322c0e636
steps:
  - prompt: assert the testdriver login page shows
    commands:
      - command: assert
        expect: the TestDriver.ai Sandbox login page is displayed
```

version: 4.0.22
steps:
  - prompt: focus chrome
    commands:
      - command: focus-application
        name: Google Chrome
        prompt: focus chrome
  - prompt: Click on google search bar
    commands:
      - command: hover-text
        text: Search Google
        description: Search Google
        action: click
  - prompt: enter text foo
    commands:
      - command: type
        text: foo
